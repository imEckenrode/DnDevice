#pragma once

/*     -  -  -     The UI      -  -  -      
    This is where the UI is initiated, at least.

    NOTE: This is very high-level, 
     since the UI itself uses lower libraries
*/

void ui_stuff_init(void);